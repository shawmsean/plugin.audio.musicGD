# 交互式分页功能说明

## 📋 功能概述

为歌曲评论功能添加了交互式分页导航，用户可以通过按钮操作在不同页之间切换，无需手动输入 URL 参数。

## 🎯 功能特性

### 1. 智能按钮显示

根据当前页面状态，动态显示不同的操作按钮：

| 当前状态 | 显示的按钮 |
|---------|-----------|
| 第一页 | 下一页、刷新当前页、退出 |
| 中间页 | 返回第1页、上一页、下一页、刷新当前页、退出 |
| 最后一页 | 返回第1页、上一页、刷新当前页、退出 |
| 无更多评论 | 刷新当前页、退出 |

### 2. 操作按钮说明

#### ⬅️ 返回第1页
- **显示条件：** 当前不是第一页
- **功能：** 快速跳转到第一页（offset=0）
- **适用场景：** 想要重新查看热门评论或从头开始浏览

#### ⬅️ 上一页 (第X页)
- **显示条件：** 当前不是第一页
- **功能：** 返回上一页（offset -= limit）
- **适用场景：** 浏览之前的内容

#### ➡️ 下一页 (第X页, 剩余Y条)
- **显示条件：** 还有更多评论未显示
- **功能：** 加载下一页评论（offset += limit）
- **适用场景：** 浏览更多评论
- **提示信息：** 显示剩余评论数量

#### 🔄 刷新当前页
- **显示条件：** 始终显示
- **功能：** 重新加载当前页评论
- **适用场景：** 查看最新评论（如有人刚发表了新评论）

#### ❌ 退出
- **显示条件：** 始终显示
- **功能：** 关闭评论对话框
- **适用场景：** 结束浏览

## 🚀 使用流程

### 场景 1：浏览多页评论

1. **打开评论**：显示第一页评论（热门评论 + 最新评论）
2. **查看评论**：阅读当前页的评论内容
3. **关闭文本查看器**：按 ESC 键或关闭按钮
4. **选择操作**：
   - 如果还有更多评论，选择"下一页"
   - 如果想看热门评论，选择"返回第1页"
   - 如果想看之前的内容，选择"上一页"
5. **重复操作**：继续浏览直到满意

### 场景 2：快速返回第一页

1. **浏览到第 5 页**
2. **关闭文本查看器**
3. **选择"返回第1页"**
4. **重新显示热门评论和第一页最新评论**

### 场景 3：刷新查看最新评论

1. **打开评论**
2. **关闭文本查看器**
3. **选择"刷新当前页"**
4. **重新加载当前页评论（可能包含新发表的评论）**

## 📊 界面示例

### 第一页界面

```
              歌曲评论 (第1页/共10页)
              总计: 500 条评论
═══════════════════════════════════════
🔥 热门评论
═══════════════════════════════════════
【1】用户A
    这首歌太好听了！
    👍 1234 点赞 | 2024-01-01

...

💬 最新评论
═══════════════════════════════════════
【1】用户B
    支持！
    👍 56 点赞 | 1小时前

...

已显示: 60/500 条评论
当前页: 1/10
═══════════════════════════════════════
```

**操作按钮：**
```
请选择操作
[0] ➡️  下一页 (第2页, 剩余440条)
[1] 🔄  刷新当前页
[2] ❌  退出
```

### 中间页界面（第5页）

```
              歌曲评论 (第5页/共10页)
              总计: 500 条评论
═══════════════════════════════════════

💬 最新评论
═══════════════════════════════════════
【201】用户C
    不错不错
    👍 23 点赞 | 2天前

...

已显示: 50/500 条评论
当前页: 5/10
═══════════════════════════════════════
```

**操作按钮：**
```
请选择操作
[0] ⬅️  返回第1页
[1] ⬅️  上一页 (第4页)
[2] ➡️  下一页 (第6页, 剩余240条)
[3] 🔄  刷新当前页
[4] ❌  退出
```

### 最后一页界面（第10页）

```
              歌曲评论 (第10页/共10页)
              总计: 500 条评论
═══════════════════════════════════════

💬 最新评论
═══════════════════════════════════════
【451】用户D
    最后一页了
    👍 12 点赞 | 3天前

...

已显示: 50/500 条评论
当前页: 10/10
═══════════════════════════════════════
```

**操作按钮：**
```
请选择操作
[0] ⬅️  返回第1页
[1] ⬅️  上一页 (第9页)
[2] 🔄  刷新当前页
[3] ❌  退出
```

## 🔧 技术实现

### 代码结构

```python
def show_song_comments(source, track_id, offset=0):
    """
    显示歌曲评论（支持分页导航）

    Args:
        source: Music source
        track_id: Track ID
        offset: Offset for pagination
    """
    # 1. 显示评论内容
    dialog.textviewer('歌曲评论', text_content)

    # 2. 构建操作按钮列表
    action_list = []

    # 添加"返回第一页"按钮（如果不是第一页）
    if current_page > 1:
        action_list.append(f'⬅️  返回第1页')

    # 添加"上一页"按钮（如果不是第一页）
    if current_page > 1:
        action_list.append(f'⬅️  上一页 (第{current_page - 1}页)')

    # 添加"下一页"按钮（如果有更多评论）
    if current_count < total:
        remaining = total - current_count
        action_list.append(f'➡️  下一页 (第{current_page + 1}页, 剩余{remaining}条)')

    # 添加"刷新当前页"按钮
    action_list.append(f'🔄  刷新当前页')

    # 添加"退出"按钮
    action_list.append('❌  退出')

    # 3. 显示操作选择对话框
    selected = dialog.select('请选择操作', action_list)

    # 4. 处理用户选择
    if selected >= 0:
        action = action_list[selected]

        if '返回第1页' in action:
            show_song_comments(source, track_id, offset=0)
        elif '上一页' in action:
            new_offset = max(0, offset - limit)
            show_song_comments(source, track_id, new_offset)
        elif '下一页' in action:
            new_offset = offset + limit
            show_song_comments(source, track_id, new_offset)
        elif '刷新当前页' in action:
            show_song_comments(source, track_id, offset)
        elif '退出' in action:
            return
```

### 关键改进

1. **移除纯文本提示**
   - 原来：`text_content += "\n提示: 可以通过 URL 参数加载更多评论\n"`
   - 现在：使用交互式按钮列表

2. **添加操作按钮**
   - 使用 `dialog.select()` 显示按钮列表
   - 根据当前状态动态生成按钮

3. **递归调用**
   - 用户选择操作后，递归调用 `show_song_comments()`
   - 传入新的 offset 参数实现分页

4. **智能按钮显示**
   - 根据当前页码和剩余评论数量
   - 动态决定显示哪些按钮

## ✅ 优势

| 方面 | 改进前 | 改进后 |
|------|--------|--------|
| 操作方式 | 手动输入 URL | 点击按钮选择 |
| 用户体验 | 需要记住 URL 格式 | 直观的按钮操作 |
| 分页导航 | 不支持 | 完整支持 |
| 刷新功能 | 需要重新打开 | 一键刷新 |
| 返回功能 | 需要手动修改 URL | 一键返回 |

## 🎨 用户体验提升

### 改进前

1. 打开评论
2. 阅读评论
3. 看到"提示: 可以通过 URL 参数加载更多评论"
4. 需要手动输入或复制 URL
5. 修改 offset 参数
6. 重新调用 URL

**问题：**
- ❌ 操作复杂
- ❌ 需要记住 URL 格式
- ❌ 容易出错
- ❌ 不直观

### 改进后

1. 打开评论
2. 阅读评论
3. 关闭文本查看器
4. 选择"下一页"按钮
5. 自动加载下一页

**优势：**
- ✅ 操作简单
- ✅ 直观的按钮界面
- ✅ 不会出错
- ✅ 用户体验好

## 🧪 测试场景

### 测试 1：基本分页

**步骤：**
1. 打开评论（第一页）
2. 选择"下一页"
3. 确认显示第二页评论
4. 选择"上一页"
5. 确认返回第一页

**预期结果：** ✅ 分页功能正常

---

### 测试 2：返回第一页

**步骤：**
1. 浏览到第 5 页
2. 选择"返回第1页"
3. 确认显示第一页评论

**预期结果：** ✅ 快速返回功能正常

---

### 测试 3：刷新当前页

**步骤：**
1. 打开评论（第一页）
2. 选择"刷新当前页"
3. 确认重新加载评论

**预期结果：** ✅ 刷新功能正常

---

### 测试 4：最后一页

**步骤：**
1. 浏览到最后一页
2. 确认没有"下一页"按钮
3. 确认有"返回第1页"和"上一页"按钮

**预期结果：** ✅ 按钮显示逻辑正确

---

### 测试 5：退出功能

**步骤：**
1. 打开评论
2. 选择"退出"
3. 确认关闭对话框

**预期结果：** ✅ 退出功能正常

## 📝 注意事项

1. **递归深度**：当前实现使用递归调用，理论上没有深度限制，但建议不要无限循环
2. **网络请求**：每次翻页都会发起新的 API 请求，请注意网络流量
3. **缓存机制**：当前没有实现缓存，每次都会重新获取数据
4. **按钮顺序**：按钮按照逻辑顺序排列，用户体验更好

## 🔮 未来改进

### 可能的改进方向

1. **添加缓存机制**
   - 缓存已加载的评论
   - 减少网络请求
   - 提高响应速度

2. **添加跳转功能**
   - 输入页码直接跳转
   - 适合快速浏览

3. **添加搜索功能**
   - 搜索评论内容
   - 搜索评论用户

4. **添加排序功能**
   - 按时间排序
   - 按点赞数排序

5. **添加评论统计**
   - 显示用户评论数
   - 显示平均点赞数

## 📚 相关文档

- [XBSWIFT2_COMPATIBILITY.md](XBSWIFT2_COMPATIBILITY.md) - xbmcswift2 兼容性文档
- [SKIN_FIX.md](SKIN_FIX.md) - 皮肤修复文档
- [QUICK_START.md](QUICK_START.md) - 快速使用指南
- [SOLUTION_SUMMARY.md](SOLUTION_SUMMARY.md) - 完整解决方案总结

---

**最后更新：** 2026-01-26
**版本：** 1.1.0
**状态：** ✅ 已完成并测试通过
