# 歌单分类选择 UI 改进

## 📋 改进内容

将歌单分类选择从弹窗选择改为文件夹形式，提升用户体验。

## 🎯 改进前后对比

### 改进前（弹窗选择）

```
1. 选择"歌单精选"
2. 弹出对话框：
   请选择歌单分类
   [0] 全部
   [1] 华语
   [2] 流行
   [3] 摇滚
   ...
3. 选择分类
4. 显示歌单列表
```

**问题：**
- ❌ 需要每次都弹出对话框
- ❌ 不能直观看到所有分类
- ❌ 返回后需要重新选择

### 改进后（文件夹形式）

```
1. 选择"歌单精选"
2. 显示分类列表（文件夹形式）：
   📁 全部
   📁 华语
   📁 流行
   📁 摇滚
   📁 电子
   📁 民谣
   ...
3. 进入分类文件夹
4. 显示该分类下的歌单列表
5. 可以随时返回分类列表
```

**优势：**
- ✅ 直观的文件夹界面
- ✅ 可以看到所有分类
- ✅ 返回后无需重新选择
- ✅ 更符合 Kodi 的操作习惯

## 🚀 使用流程

### 新的浏览流程

```
1. 打开插件主菜单
2. 选择"歌单精选"
3. 显示分类列表（文件夹形式）
4. 选择分类（如：华语）
5. 显示该分类下的高质量歌单列表
6. 选择感兴趣的歌单
7. 显示歌单中的歌曲列表
8. 选择歌曲播放或点击"播放全部"
```

### 界面示例

**分类列表（文件夹形式）：**
```
歌单分类
├── 全部
├── 华语
├── 流行
├── 摇滚
├── 电子
├── 民谣
├── 说唱
├── 古典
├── 轻音乐
├── 爵士
└── ...
```

**歌单列表（进入"华语"后）：**
```
『异域幻想』妖精乡 · 骑士的放浪冒险谭 - 花嫁赫禄
创建者: 花嫁赫禄
歌曲数: 172
播放量: 4388605

地下酒吧｜爵士与调酒师的香醇情调 - 花嫁赫禄
创建者: 花嫁赫禄
歌曲数: 83
播放量: 4618137

...

下一页 >>
<< 返回分类
```

## 🔧 技术实现

### 代码改进

**改进前：**
```python
def show_playlist_tags():
    """显示歌单标签列表"""
    dialog = xbmcgui.Dialog()

    # 获取标签列表
    tags_data = get_playlist_tags()

    if not tags_data:
        dialog.notification('错误', '获取歌单标签失败')
        return

    tags = tags_data.get('tags', [])

    # 构建标签列表
    tag_list = []
    tag_list.append('全部')

    for tag in tags:
        tag_list.append(tag.get('name'))

    # 显示标签选择对话框
    selected = dialog.select('请选择歌单分类', tag_list)

    if selected >= 0:
        selected_tag = tag_list[selected]
        show_highquality_playlists(cat=selected_tag)
```

**改进后：**
```python
def show_playlist_tags():
    """显示歌单标签列表（文件夹形式）"""
    log('Showing playlist tags')

    # 获取标签列表
    tags_data = get_playlist_tags()

    if not tags_data:
        dialog = xbmcgui.Dialog()
        dialog.notification('错误', '获取歌单标签失败')
        return

    tags = tags_data.get('tags', [])

    # 添加"全部"选项
    add_directory_item('全部', get_url(mode='highquality_playlists', cat='全部'))

    # 添加各个标签
    for tag in tags:
        tag_name = tag.get('name', '')
        tag_id = tag.get('id')

        # 构建标签 URL
        url = get_url(mode='highquality_playlists', cat=tag_name)

        # 添加到列表
        add_directory_item(tag_name, url, is_folder=True)
```

### 关键改进点

1. **移除弹窗对话框** - 不再使用 `dialog.select()`
2. **使用文件夹列表** - 使用 `add_directory_item()` 创建文件夹
3. **保留分类选择** - 通过 URL 参数 `cat` 传递分类信息
4. **保持返回导航** - 在歌单列表中添加"返回分类"按钮

## ✅ 优势

| 方面 | 改进前 | 改进后 |
|------|--------|--------|
| 选择方式 | 弹窗选择 | 文件夹选择 |
| 用户体验 | 需要每次弹窗 | 直观的文件夹界面 |
| 分类可见性 | 一次只能看到一个列表 | 可以看到所有分类 |
| 返回操作 | 需要重新选择 | 直接返回分类列表 |
| 操作习惯 | 不符合 Kodi 习惯 | 符合 Kodi 习惯 |
| 浏览效率 | 低 | 高 |

## 📊 用户体验提升

### 场景 1：浏览多个分类

**改进前：**
1. 选择"歌单精选"
2. 弹窗选择"华语"
3. 浏览歌单
4. 返回
5. 选择"歌单精选"
6. 弹窗选择"流行"
7. 浏览歌单

**改进后：**
1. 选择"歌单精选"
2. 进入"华语"
3. 浏览歌单
4. 返回分类列表
5. 进入"流行"
6. 浏览歌单

**优势：** 无需重复打开弹窗，操作更流畅

### 场景 2：发现新分类

**改进前：**
- 需要记住分类名称
- 在弹窗中查找

**改进后：**
- 可以看到所有分类
- 直接浏览发现

**优势：** 更容易发现新的音乐风格

## 🎯 使用建议

### 1. 浏览不同分类

使用文件夹形式，可以轻松浏览不同的歌单分类，发现新的音乐风格。

### 2. 快速返回

在歌单列表中点击"<< 返回分类"，可以快速返回分类列表，继续浏览其他分类。

### 3. 探索新音乐

由于所有分类都可见，可以更容易地探索和发现新的音乐类型。

## 📝 版本历史

- **v1.2.0** (2026-01-26): 初始版本，使用弹窗选择
- **v1.2.1** (2026-01-26): 改进为文件夹形式

---

**最后更新：** 2026-01-26
**版本：** 1.2.1
**状态：** ✅ 已完成并测试通过
